version: '3'

services:

  backend:
    build: .
    ports:
      - "8000:8000"

  postgres:
    image: postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password

  redis:
    image: redis

  mongodb:
    image: mongo

  kafka:
    image: bitnami/kafka